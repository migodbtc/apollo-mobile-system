// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
  }
}

def reactNativeAndroidDir = new File(
  providers.exec {
    workingDir(rootDir)
    commandLine("node", "--print", "require.resolve('react-native/package.json')")
  }.standardOutput.asText.get().trim(),
  "../android"
)

allprojects {
  repositories {
    maven {
      // All of React Native (JS, Obj-C sources, Android binaries) is installed from npm
      url(reactNativeAndroidDir)
    }

    google()
    mavenCentral()

    maven { url 'https://www.jitpack.io' }
  }
  // Force normalized BlurView artifact coordinate as a fallback in case
  // a transitive dependency requests an unusual coordinate like
  // 'com.github.Dimezis:BlurView:version-2.0.6' which some resolvers
  // may expose as 'BlurView-version-2.0.6.aar' on JitPack and cause 500s.
  // This forces Gradle to request the standard '2.0.6' version instead.
  configurations.all {
    resolutionStrategy {
      force 'com.github.Dimezis:BlurView:2.0.6'
    }
  }
}

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
